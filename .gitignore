/target
.env
node_modules
web/.svelte-kit
# wrangler dev writes bundles and local state here.
web/.wrangler
web/build
*.log
# Feature worktrees live inside the repo; never stage their contents.
.worktrees/
.vscode/
